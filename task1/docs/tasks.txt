** TODO **

 * Без передачи, восстанавливая глубину по псевдо глубине. Для получени псевдо глубины растеризуемого 
    фрагмента следует пользовать встроенной в GLSL переменной gl_FragCoord (только для фрагментного шейдера). 
    gl_FragCoord.z содержит значение псевдо глубины в диапазоне [0;1] (NB!, это значение необходимо перевести в [-1;1], 
    как при выводе матрицы проекции). 

** DONE **
* Отобразить в качестве цвета модели приведенную линейную глубину от камеры до растеризуемого фрагмента. 
  Т.е в цвет надо вывести (-z - near) / (far - near) , где z - координата растеризуемого фрагмента в 
  системе координат камеры. Сделать надо двумя способами:
  * Явной передачей z координаты с вершинного на фрагментный шейдер. 
* Для уменьшения эффекта z-fighting'а следует рисовать каркас со смещением (glPolygonOffset).
* Отобразить каркас поверх модели. 
  Реализуется повторной отрисовкой модели с режимом каркасной заливки 
  (glPolygonMode(GL_FRONT_AND_BACK, GL_LINE)) с функцией GL_LESS_EQUAL для теста глубины. 
* Переписать заново код
* Wireframe white color
* Отобразить модель формата OBJ (для сверки корректности отображения моделей можно использовать Blender). 
* tutorial 4
* nbproj
